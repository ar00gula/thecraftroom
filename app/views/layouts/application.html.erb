<!DOCTYPE html>
<html>
  <head>
    <title>Crafting</title>
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Epilogue:ital,wght@0,400;0,700;1,400&display=swap" rel="stylesheet">
    
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
      <div class="navbar-ul">
      <% if current_user%>
        <div class="li"><a href=<%=home_path%>>My Craft Room</a></div>
        <div class="li dropdown"> <a href=<%=craft_categories_path%>>Crafts</a>
          <div class="dropdown-content">
            <a href=<%=craft_categories_path%>>Craft Categories</a>
            <a href=<%=crafts_path%>>All Crafts</a>
            <a href=<%=new_craft_path%>>Create New Craft</a>
         </div>
        </div>
        <div class="li dropdown">
          <a href="/supplies">Supplies</a>
          <div class="dropdown-content">
            <a href=<%=supply_categories_path%>>Supply Categories</a>
            <a href=<%=supplies_path%>>All Supplies</a>
            <a href=<%=new_supply_path%>>Add New Supply</a>
         </div></div>
        <div class="li"><a href=<%=leaderboard_path%>>Leaderboard</a></div>
        <div class="li"><a href=<%=user_path(current_user)%> data-confirm="Are you sure you wish to logout?" data-method="delete">Log Out</a></div>
      <% end %>
      <% if !current_user%>
        <div class="li"><a href="/login">Log In</a></div>
        <div class="li"><a href="/signup">Sign Up</a></div>
      <% end %>
    </div>
    <div class="wrapper">
      <%= yield %>
    </div>
  </body>
</html>